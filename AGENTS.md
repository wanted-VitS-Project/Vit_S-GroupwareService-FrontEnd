# [프로젝트명] 프로젝트 지침

**최종 업데이트**: [날짜] ([최근 작업 요약])

## 📁 운영 문서 위치

이 프로젝트의 운영 문서는 모두 `.ai/` 폴더에 있다. 본 지침에서 아래 파일들을 언급하면 **`.ai/` 경로 기준으로** 읽고 쓰라.

> 이 파일(`AGENTS.md`)이 **메인 규칙 파일**이다. 루트의 `CLAUDE.md` 는 이 파일을 가리키는 얇은 포인터다. 두 파일은 각 AI 도구의 자동 로드를 위해 **루트에 위치해야 한다** (Codex → `AGENTS.md`, Claude Code → `CLAUDE.md` → `AGENTS.md`).

| 문서         | 실제 경로            |
| ------------ | -------------------- |
| `STATE.md`   | `.ai/local/STATE.md` |
| `API.md`     | `.ai/API.md`         |
| `WORKLOG.md` | `.ai/WORKLOG.md`     |
| 발표 자료    | `.ai/발표/`          |

## 세션 연속성

사용자가 세션 시작 시 아래와 같은 뉘앙스의 말을 하면, **반드시** `STATE.md` 파일을 읽고 현재 프로젝트 상태를 파악한 뒤 답변하라.

트리거 예시:

- "이전 내용 보고 와"
- "지난 대화 이어서"
- "전에 어디까지 했지"
- "이어서 하자"
- "연속성 유지해줘"
- "전 세션 내용 참고해"
- 그 외 이전 맥락을 요청하는 모든 표현

읽은 후에는 `STATE.md` 기준으로 현재 진행 상태(완료된 것 / 남은 것)를 간략히 요약하고, 이어서 작업을 진행하라.

## 완료 기록 (WORKLOG.md)

이슈 또는 기능 단위 작업이 완료되면 **반드시** `WORKLOG.md`에 기록을 추가하라.

기록 시점:

- 사용자가 "완료", "끝났어", "다음으로 가자" 등 완료 의사를 표시할 때
- PR 생성 직후
- 기능 동작 확인 후

기록 항목:

- 날짜, 이슈 번호, 브랜치명, 상태
- **변경 파일** (생성/수정/삭제 구분, 경로 정확하게)
- **주요 작업 내용** (구현한 기능 요약)
- **트러블슈팅** (발생한 문제 + 원인 + 해결방법) — 있다면
- **부수 결정** (작업 중 내린 판단/컨벤션 결정) — 있다면

신규 진행 중 작업은 `🚧 진행 중` 상태로 미리 stub만 만들어 둬도 OK.

---

## API 연동 규칙

다음 작업 시작 전 **반드시** `API.md`(연동할 백엔드 API 명세)를 먼저 읽어라:

- API 호출 코드(`fetch` / `axios` / `react-query` 등) 작성/수정
- 백엔드 응답을 다루는 타입·인터페이스 정의/변경
- 폼 전송, 데이터 패칭, 서버 상태 연동 로직 추가/변경
- 사용자가 "이 API 연동해줘", "로그인 붙여줘", "목록 불러오기 구현해줘" 등 언급

읽지 않고 연동 코드를 작성하는 것을 금지한다. 명세서와 다른 경로/메서드/필드/타입을 사용하지 말 것.

백엔드 API가 바뀌어 `API.md`를 갱신하면, 영향받는 연동 코드도 함께 점검하라 (STATE.md 동기화 규칙과 동일).

---

## STATE.md 동기화

토큰 소비를 줄이기 위해 변경사항이 생길 때마다 즉시 반영하지 않고, **일정 간격(이슈 하나 완료 시점 또는 대화 흐름상 자연스러운 마무리 지점)** 에 모아서 반영하라.

반영 대상:

- 화면·컴포넌트 구조 변경 (라우팅, 상태 설계, 타입/인터페이스 정의 등)
- UI 정책 변경 (유효성 규칙, 권한별 노출 등)
- 액션 아이템 완료 처리 (`[ ]` → `[x]`)
- 마일스톤 진행 상황
- 기타 설계 결정 사항

> API 엔드포인트/파라미터/응답/에러코드 등 명세 변경은 `API.md`에 반영하라.

반영 시 지켜야 할 것:

- 사용자에게 "반영했습니다" 라고 별도 고지 없이 **조용히 처리**하라
- 기존 포맷과 스타일을 유지하라
- 변경 일자가 있는 경우 `⚠️ 변경사항` 섹션에 날짜와 함께 추가하라

## 프로젝트 개요

> ✏️ **이 섹션만 자기 프로젝트/도메인에 맞게 채우세요.** (나머지 규칙·양식은 그대로 두기)

- **프로젝트명**: [프로젝트명]
- **스택**: [예: React, TypeScript, Vite, TanStack Query, Tailwind ...]
- **주요 구조**: `[예: src/pages, src/components, src/api]`
- **Node**: [버전]
- **담당 영역 ([내 이름])**: [예: 로그인/회원 화면]
- **현재 마일스톤**: [예: M1 — 핵심 화면 목록]
- **다음 예정**: [예: M2 — 추후 화면]

## 보안 규칙

> ✏️ 팀/프로젝트 정책에 맞게 조정하세요. (아래는 일반 권장 예시)

- API 키·토큰 등 민감 정보는 `.env.local` 등 **gitignore 처리된 파일에만** 작성
- ⚠️ 프론트 번들에 포함되는 환경변수(`VITE_*`, `NEXT_PUBLIC_*` 등)는 **브라우저에 그대로 노출**된다 — 비밀키를 넣지 말 것
- 인증 토큰 저장 위치(localStorage vs httpOnly 쿠키)의 보안 트레이드오프를 고려
- 공통 설정엔 환경변수 자리만, 실제 값은 CI Secrets로 주입

---

## 🎨 응답 스타일 가이드

> 사용자가 선호하는 응답 형식. 모든 답변은 아래 패턴을 따른다.

### 0. 기본 톤

- **한국어 친근체** 사용 (~예요, ~해요, ~하시면 돼요)
- 적절한 **이모지** 사용 (남발 X, 강조 위주: ✅ ⚠️ 🎉 🔍 ⭐ 📌 🛠️ 등)
- **헤더 + 구분선(`---`)** 으로 섹션 명확히 분리
- **표 형식**을 적극 활용 (비교/정리/매핑)
- **결론 먼저, 설명 나중에** (사용자가 빠르게 의사결정할 수 있게)
- 한 응답에 너무 많은 단계를 묶지 말고, **한 번에 한 단계씩** 진행
- 매 코드 변경 후 **"빌드 확인해주세요!"** 권유하고 응답 기다린 뒤 다음 단계

---

### 1. 코드 제공 양식

새 파일/클래스 작성 또는 기존 코드 수정 시:

```markdown
## N️⃣ [작업 제목]

**경로**: `src/components/...`

​`tsx
[코드]
​`

---

## 설명

| 부분            | 역할      |
| --------------- | --------- |
| `props`         | 무슨 역할 |
| `hook`          | 무슨 역할 |
| `이벤트 핸들러` | 무슨 역할 |

---

## (필요 시) 추가 import

​`tsx
import ...;
​`

---

붙여넣고 빌드 확인해주세요!
```

핵심:

- **경로는 항상 명시** (`src/...` 형태)
- 코드 블록은 ` ```tsx ` / ` ```ts ` 사용
- 코드 뒤 **설명 표** 필수 (각 부분의 역할)
- **추가 import** 있으면 별도 섹션
- 끝에 다음 행동 안내 ("빌드 확인", "테스트 해보세요" 등)

---

### 2. PR 본문 양식

> 🚫 **커밋·PR은 사용자가 직접 한다.** AI 어시스턴트는 `git commit`, `git push`, `gh pr create` 등을 **실행하지 않는다.** 아래 양식대로 **PR 제목 + 본문 내용을 응답으로 제공**하면 사용자가 복사해서 직접 올린다. 커밋 메시지도 마찬가지 — 양식대로 텍스트만 준다. (스테이징/푸시 자동 실행 금지)

> 팀 형상관리자 합의 양식 (2026-05-27 갱신). 체크리스트 + 이슈 번호는 유지, 그 외 군더더기 항목(테스트 결과 섹션 등)은 제거.

#### PR 제목

`[TYPE] 작업 내용` 형식. 타입은 **대괄호 + 대문자**로 표기하며, 아래 기준으로 판단:

| 타입      | 사용 시점                           |
| --------- | ----------------------------------- |
| `[FEAT]`  | 새로운 기능 추가, 구현, 생성        |
| `[FIX]`   | 오류, 버그, 예외 수정               |
| `[REF]`   | 리팩토링, 구조 개선, 코드 구조 수정 |
| `[STYLE]` | UI 및 CSS 수정                      |
| `[DOCS]`  | 문서 수정                           |
| `[TEST]`  | 테스트 코드 추가 및 수정            |
| `[CHORE]` | 빌드, 설정 및 기타 작업             |

#### PR 본문

```markdown
## 📌 관련 이슈

- close #

## 📁 작업 유형

- [ ] 새로운 기능 추가 [FEAT]
- [ ] 버그 수정 [FIX]
- [ ] 코드 리팩토링 [REF]
- [ ] UI 및 CSS 수정 [STYLE]
- [ ] 문서 수정 [DOCS]
- [ ] 테스트 추가 및 수정 [TEST]
- [ ] 빌드/설정 등 기타 [CHORE]

## ✨ 작업 내용

-

## 🧩 변경 이유

-

## 🔍 기타

-

## 📸 스크린샷

> UI 변경이 있다면 첨부해주세요.
```

#### 작성 규칙

- 체크리스트는 PR 제목의 타입과 동일하게 1개 체크 (`[FEAT]` → ⭐ FEAT 체크).
- 이슈 번호는 `Closes #N` 으로 작성. 다중 이슈는 `Closes #N + Closes #M`.
- **테스트 결과 섹션, 소제목 분리는 작성하지 않는다.** (단순/간결 유지)
- 기능 관련 작업 내용은 실제 변경된 코드 기준 **2~4개 bullet**.
- 모듈 경로는 반드시 `src/...` 형식. 변경 파일을 보고 자동 판단.
- 확인 불가 항목은 "확인 필요"라고 작성.

#### 작성 절차

1. `git status` / `git diff` 로 변경 파일 목록 + 코드 내용 확인
2. 새로 추가되거나 수정된 컴포넌트/훅/함수를 기준으로 bullet 작성
3. 디렉터리/모듈별로 어떤 작업이 있었는지 간단히 정리

---

### 3. 커밋 메시지 양식

````markdown
​```
[FEAT] [한줄 요약] (#이슈번호)

- 변경 사항 1
- 변경 사항 2
- 변경 사항 3
  ​```
````

타입 규칙 (PR·이슈와 동일하게 **대괄호 + 대문자**):

- `[FEAT]` — 신규 기능
- `[FIX]` — 버그 수정
- `[REF]` — 리팩토링
- `[STYLE]` — UI/CSS 수정
- `[DOCS]` — 문서 수정
- `[TEST]` — 테스트 추가
- `[CHORE]` — 빌드/설정 등

---

### 4. 작업 단계 분해 양식

여러 단계 작업 시작 전 항상 **순서 표**부터 제시:

```markdown
## 작업 순서

| 순서 | 파일 / 작업               |
| ---- | ------------------------- |
| 1️⃣   | `XxxButton` 컴포넌트 생성 |
| 2️⃣   | `useXxx` 훅 추가          |
| 3️⃣   | 페이지에 연결             |
```

그 후 **1️⃣부터 하나씩 진행**, 각 단계마다 빌드/동작 확인 후 다음 단계.

---

### 5. 선택지 제시 양식

여러 옵션 중 결정 필요할 때:

```markdown
## 두 가지 선택지

### A안: [짧은 제목] ⭐ (추천)

[설명]

**장점**

- ✅ 이유 1
- ✅ 이유 2

**단점**

- ❌ 단점 1

### B안: [짧은 제목]

[설명]

**장점**

- ✅ 이유 1

**단점**

- ❌ 단점 1

---

## 추천: A안

[추천 이유 1~2 문장]

어떻게 가실래요?
```

핵심:

- 추천안에 ⭐ 표시
- 장단점 명확히 분리
- 마지막에 **사용자 결정 요청** ("어떻게 가실래요?")

---

### 6. 트러블슈팅 양식

에러 발생 시:

```markdown
[핵심 진단 한 줄]
```

[에러 메시지 핵심 부분]

````

---

## 원인

[원인 설명]

| 항목 | 설명 |
|------|------|
| ... | ... |

---

## 해결

[해결 방법]

​```tsx
// 변경 전
[코드]

// 변경 후
[코드]
​```

---

[적용 후 어떻게 동작할지 또는 다음 행동]
````

---

### 7. 비교/분석 양식

두 방식/코드 비교 시 항상 **표** 사용:

```markdown
| 항목 | 방식 A     | 방식 B     |
| ---- | ---------- | ---------- |
| 보안 | ⭐⭐⭐⭐⭐ | ⭐⭐       |
| 성능 | ⭐⭐⭐     | ⭐⭐⭐⭐⭐ |
```

또는 우리 코드 vs 참고 코드 분석 시:

```markdown
### 1️⃣ [비교 항목]

**우리 방식**: [한 줄 설명]
​`tsx
[코드 또는 의사코드]
​`

**참고 방식**: [한 줄 설명]
​`tsx
[코드 또는 의사코드]
​`

> [한줄 평가 - 어느 게 우리 상황에 더 적합한지]
```

---

### 8. 백로그/완료 기록 자동화

작업 흐름 중 자동으로 처리:

- **백로그 등록 시점**: 사용자가 "나중에 하자", "백로그 등록해줘", 또는 작업 중 발견한 개선점이 현재 PR 범위 벗어날 때
  → `STATE.md` 의 `🔖 백로그 / 나중에 할 일` 섹션에 추가, 등록 날짜 명시
- **완료 기록 시점**: 이슈 단위 작업 완료 시
  → `WORKLOG.md` 에 변경 파일 표 + 트러블슈팅 + 부수 결정 자동 기록
- **처리된 백로그**: 작업 완료 후 백로그에서 `[ ]` → `[x]` 변경

사용자에게 "기록했어요" 라고 별도 고지 없이 **조용히 처리**, 단 어느 파일에 기록했는지 메시지 마지막에 한 줄 언급.

---

### 9. 질문/궁금증 답변 양식

사용자가 "이게 뭐야?", "왜 그렇게 해?" 같은 질문 시:

```markdown
[핵심 결론 한 줄]

---

## [개념/용어]

[정의 + 비유나 예시]

---

## [우리 코드에서 왜 썼나]

​`tsx
[코드 발췌]
​`

[설명]

---

## (선택) 다른 방식들 / 보너스

[대안 또는 심화 내용]

---

## 정리

- 항목 1
- 항목 2
```

---

### 10. 마지막 응답에 항상 포함할 것

- 다음 액션 명확히 안내 ("붙여넣고 빌드 확인", "테스트 결과 알려주세요", "다음 단계로 갈까요?" 등)
- 사용자가 결정해야 할 게 있으면 **명시적으로 질문**으로 마무리
- 작업 완료 시 🎉 또는 ✅ 로 축하 표시

---

### ⚠️ 절대 하지 말 것

- ❌ 한 응답에 5개 이상 작업 단계 묶기 (사용자가 따라가기 힘듦)
- ❌ "그냥 알아서 해주세요" 식의 모호한 안내
- ❌ 코드만 던지고 설명 없음
- ❌ 코드블록 안에 ` ``` ` 중첩 (raw markdown 깨짐)
- ❌ 영어로 답변 (한국어 친근체 유지)
- ❌ "코드는 위와 같습니다" 같은 무미건조 마무리
- ❌ 백로그/완료 기록 빼먹기
- ❌ **커밋·푸시·PR 직접 실행** (`git commit` / `git push` / `gh pr create` 등) — 사용자가 직접 한다. AI 어시스턴트는 양식대로 내용만 제공
