# [프로젝트명] — Claude Code 진입점

이 프로젝트의 AI 협업 규칙·응답 스타일·운영 문서 체계는 모두 메인 규칙 파일 **`AGENTS.md`** 에 정의되어 있다.

Claude Code 는 아래 import 를 통해 `AGENTS.md` 내용을 그대로 로드하고 따른다. (Codex 등 다른 도구는 `AGENTS.md` 를 직접 자동 로드한다.)

> ⚠️ 규칙을 수정할 때는 이 파일이 아니라 **`AGENTS.md` 를 편집**하라. 이 파일은 포인터일 뿐이다.

@AGENTS.md
