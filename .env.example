# 이 파일을 복사해 .env.local 로 만들고 값을 채운다. (.env.local 은 gitignore 처리됨)
#
#   cp .env.example .env.local
#
# ⚠️ NEXT_PUBLIC_ 접두사가 붙은 값은 브라우저 번들에 그대로 포함된다. 비밀키를 넣지 말 것.

# 백엔드 API 서버 주소 (끝에 슬래시 없이)
# 비어 있으면 프론트 서버로 요청이 가서 404 가 난다.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
